name: Reveal Secret Value
on: workflow_dispatch  # This allows you to run it manually

jobs:
  reveal:
    runs-on: ubuntu-latest
    steps:
      - name: Print Secret with Spaces
        # The sed command adds a space after every character to bypass masking
        run: echo "${{ secrets.API_KEY }}" | sed 's/./& /g'